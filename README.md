# agentic-accelerator-intake

Агентный первый фильтр заявок в стартап-акселератор, собранный в [Langflow](https://langflow.org).
Ассистент за шесть–восемь вопросов выясняет, стоят ли за идеей факты, проверяет
рынок и выдаёт вердикт, посчитанный детерминированным кодом, вместе с адресным
домашним заданием.

Это агентная переработка исходного питоновского проекта `ai-accel`: шестнадцать
узлов ручной обвязки свёрнуты в шесть, а вся логика интервью перенесена в
инструкции агента.

---

## Что внутри

| Папка | Содержимое |
|---|---|
| `flow/` | Экспорт флоу для импорта в Langflow. Ключи и идентификаторы вычищены. |
| `components/` | Исходный код трёх кастомных компонентов отдельными файлами, чтобы их можно было читать и ревьюить как обычный Python. |
| `docs/` | Документация: архитектура, находки, компромиссы, тезисы для демо. |
| `presentation/` | Презентация на 19 слайдов и BPMN-схема потока. |

## Архитектура

```
Chat Input ──────────────► Agent ──► Chat Output
Yandex Chat Model ───────►  │  (model)
Verdict R1-R5 (tool) ────►  │  (tools)
Yandex Web Search (tool) ─►  │  (tools)
```

Шесть узлов и пять рёбер. Ключевой принцип — **модель говорит, а код решает**:

- **Agent** ведёт интервью. Персона, четыре темы, рубрика оценок, запреты и бюджет
  вопросов живут в его инструкциях. Память диалога встроена в компонент
  (`n_messages`), поэтому отдельные узлы хранения состояния не нужны.
- **Verdict R1-R5** — детерминированный код, вызывается как инструмент
  `compute_verdict`. Правила пропуска заявки не отданы модели: одинаковые оценки
  на входе всегда дают одинаковый вердикт, и его можно перепроверить по логу.
- **Yandex Web Search** — инструмент `search_market`, возвращает реальные ссылки
  из поисковой выдачи, а не выдуманные моделью названия.
- **Yandex Chat Model** — обёртка над OpenAI-совместимым эндпоинтом Yandex AI
  Studio, типизированная как `LanguageModel`, чтобы её мог использовать Agent.

## Методология

Четыре темы интервью опираются на известные рамки: Customer Development
(аудитория), The Mom Test (поведение), Jobs to Be Done (альтернативы) и Lean
Startup (проверка гипотезы). По каждой теме ставится оценка 0, 1 или 2, и дальше
проходит только тот, у кого все четыре темы закрыты на двойку.

Подробности — в [`docs/AGENTIC-WORKFLOW-RU.md`](docs/AGENTIC-WORKFLOW-RU.md).

## Как запустить

1. Поднимите Langflow (проверялось на сборке с Python 3.14 в Docker).
2. Импортируйте `flow/interview-bot-agentic.json` через **Upload a flow**.
3. Впишите свои значения в поля, которые были вычищены перед публикацией:
   - `Yandex Chat Model` → **Yandex API Key**, **Yandex Folder ID**
   - `Yandex Web Search` → **Yandex API Key**, **Yandex Folder ID**
4. Откройте **Playground** и напишите идею обычным текстом.

Langflow вырезает значения полей типа `SecretStrInput` при импорте и создании
флоу через API, поэтому ключ в любом случае придётся ввести руками.

### Модель

По умолчанию стоит `deepseek-v4-flash/latest`. Выбор не косметический: на
`yandexgpt/latest` ассистент переставал соблюдать правило двух попыток на тему и
проскакивал темы, укладывая интервью в четыре вопроса вместо шести–восьми.
Сравнение всех доступных моделей — в документации.

Запасной вариант: `qwen3.6-35b-a3b` — в сравнении вёл себя так же чисто и
обходится примерно в полтора раза дешевле.

## Стоимость

Замеренное интервью из шести ходов: 33 965 входящих и 2 232 исходящих токена.
По тарифам Yandex AI Studio для DeepSeek V4 Flash это около 12 ₽ за заявку
вместе с одним вызовом веб-поиска, который тарифицируется отдельно.

Верхняя граница задана архитектурой, а не тарифом: бюджет в десять вопросов и
лимит в две попытки на тему не дают разговору расползтись, поэтому заявка не
может стоить дороже примерно 17 ₽.

## Честные ограничения

- Оценки по темам ставит модель. Сами правила вердикта детерминированы, но входы
  в них — нет, поэтому одну и ту же расшифровку модель может оценить по-разному.
- Соблюдение правил интервью держится на качестве модели, а не на жёсткой
  проверке в коде. Это измерено и описано в документации.
- Размеченного датасета пока нет, поэтому согласованность оценок не измерена.
  Это следующий шаг и условие промышленного использования.

## Безопасность

В репозитории нет ключей. Перед публикацией из экспорта флоу удалены значения
`api_key`, `folder_id` и закешированный список моделей аккаунта.

## Документация

- [`docs/AGENTIC-WORKFLOW-RU.md`](docs/AGENTIC-WORKFLOW-RU.md) — архитектура,
  находки, компромиссы, сравнение моделей.
- [`docs/DEMO-TEZISY-RU.md`](docs/DEMO-TEZISY-RU.md) — тезисы для демо
  представителям акселератора.
- [`docs/WORKFLOW.md`](docs/WORKFLOW.md) — технические заметки и история отладки
  исходного флоу на шестнадцать узлов, включая грабли Langflow.
- [`presentation/presentation-ru.html`](presentation/presentation-ru.html) —
  презентация на 19 слайдов, открывается в браузере.
